fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
# Multipart request parsing, which FastAPI requires for `UploadFile`. A RUNTIME
# dependency, not a test one: without it the document upload route raises at
# import time in production. Added by decision 012, which reverses
# `master_health/__init__.py`'s "no upload endpoint" in the open — that note
# calls adding one "a dependency decision", and this is the dependency.
python-multipart>=0.0.9

# Commercial Decision Platform — persistence (Phase 1 foundation)
SQLAlchemy>=2.0
alembic>=1.13
# Postgres driver for production; dev/test default to SQLite (stdlib, no driver).
psycopg[binary]>=3.1
# Encrypts each organization's Zoho client secret/refresh token at rest.
cryptography>=41.0

# pie-parser (the pinned clone at ../pie-parser) pulls these in:
pyyaml>=6.0
openpyxl>=3.1

# Outbound HTTP: the Zoho client, the ERP connector transports, the AI
# providers, and the single sign-on discovery fetch. Listed here rather than
# under `# tests` where it used to sit — it is imported at module scope by
# `app/oauth.py` and `app/sso/verify.py`, so a deployment that trusted the
# heading and installed without it would fail at import, not in a test run.
httpx>=0.27

# tests
pytest>=8.0

# Verifying an identity provider's ID token on the single sign-on path.
# Declared rather than relied on as a transitive dependency: this is the
# security boundary of that feature, and the failure modes it closes — `alg:
# none`, an RS256 token re-signed HS256 against the public key — are exactly
# what a hand-rolled sixty lines gets wrong. 2.4 is the floor that requires
# `algorithms=` explicitly (CVE-2022-29217); this asks for more than that.
PyJWT>=2.7
