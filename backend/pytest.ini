[pytest]
testpaths = tests
# The live-provider contract suite (WS3) calls a real model and costs money, so
# it is excluded from the default run. Opt in with:  pytest -m live
addopts = -m "not live"
markers =
    live: exercises a real AI provider; requires credentials; excluded by default
