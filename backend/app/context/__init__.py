"""Context Assembler: turn a deterministic signal + read model into the curated,
permission-scoped ``ContextBundle`` that is the ONLY thing the AI ever sees.

Redaction happens here (RESTRICTED facts are removed, not masked), so restricted
economics never reach the AI, the API, or a salesperson-scoped surface.
"""
